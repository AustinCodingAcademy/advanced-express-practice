import React, { Component } from "react";


class Contact extends Component (props) {

  componentDidMount() {
    /*
    let myId = props.params.id;
    console.log(myId);
    props.getContact(myId);
    */
  }
  render() {
    return (
      <div> hello Contact</div>
    );
  }
  
}
export default Contact;
