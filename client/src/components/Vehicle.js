import React, { Component } from "react";


class Vehicle extends Component (props) {

  componentDidMount() {
    /*
    let myId = props.params.id;
    console.log(myId);
    props.getVehicle(myId);
    */
  }
  render() {
    return (
      <div> hello vehicle</div>
    );
  }
  
}
export default Vehicle;
